# 白藤（rattan）模型来源说明

本目录由仓库根目录的 `白藤/rattan/` 模型包导入。运行时保留 moc3、物理、
参数说明、待机动画和表情文件；五张 4096 纹理缩小为 2048，用于降低桌宠的
显存占用。`rattan.model3.json` 是 VoiceMem 的运行时清单，补充了嘴型分组、
待机动作和原模型包中的表情引用。

原模型包没有附带明确的再分发许可证，并在 VTube Studio 配置中标记了禁止
Workshop 分享。发布源码、安装包或其他包含这些模型文件的产物前，必须由模型
权利人确认授权范围；不要把本说明当作模型许可证。
